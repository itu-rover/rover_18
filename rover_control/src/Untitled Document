
imu_publisher = imu datasını alır, ekf localization için güzel hale getirir.
gps_handler=gps verisini ekf için güzel hale getirir

waypoint_handler = verilen gps koordinatını heading ve bearinge çevirir, belirli mesaj tipini atar.



rover_autonom = imu_gps_handler ile çalışır. movebase'a parametre veriyor.

rover_garen2_control = simulasyondan verileri alıp odoma çevirir.

rover_real_time_control2 = roverı bilinçsizce yürütür. odom verisi almadan.
rover_real_time_control3 = imudan veri alır, roveri yürütür.

